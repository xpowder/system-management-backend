[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Same as Dockerfile CMD. Do not use createsuperuser or migrate-only here.
startCommand = "/app/start.sh"
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
